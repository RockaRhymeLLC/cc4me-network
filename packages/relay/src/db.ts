/**
 * SQLite database — schema and query helpers.
 *
 * Uses better-sqlite3 for synchronous, fast SQLite access.
 * Database stored on local disk (never network filesystem).
 */

// TODO: implement schema from spec (agents, contacts, email_verifications, admins, broadcasts, rate_limits)
// See specs/phase-1.md "Relay Database Schema" section

export function initializeDatabase(_dbPath: string): void {
  // TODO: implement
}
